// ⚠ OBSOLETO — el seed se ha movido a `src/db/seed.ts` para que entre en la
// compilación TypeScript y pueda ejecutarse en producción sin ts-node.
//
// Usa los scripts npm:
//   npm run db:seed       → ejecuta dist/db/seed.js (runtime, Docker prod/dev)
//   npm run db:seed:dev   → ejecuta src/db/seed.ts con ts-node (desarrollo local)
//
// Puedes borrar este archivo cuando quieras: rm prisma/seed.ts
